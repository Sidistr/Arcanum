# Arcanum
What if your source code could never be stolen — not because it's hidden, but because it mathematically cannot be read? Arcanum is a ZK-based programming language concept exploring exactly that.
